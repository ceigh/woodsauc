image: node:latest

#tests:
#  stage: tests
#  script:
#    - echo 'Tests passed!'

production:
  stage: production
  script:
    - npm i --progress=false
    - npm run 'to production'
  artifacts:
    paths:
      - public
